export enum AppMode {
  Menu = 'menu',
  DivSelect = 'div-select',
  Game = 'game',
  Summary = 'summary',
  Leaderboard = 'leaderboard',
}

export enum MathMode {
  Add = 'add',
  Sub = 'sub',
  Div = 'div',
  Mul = 'mul',
  Mixed = 'mixed',
}